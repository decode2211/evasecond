# AI-assistant / tool state (not part of the project)
.claude/
CLAUDE.md
.cursor/
.aider*

# Backend build output and Go test artifacts
backend/server
backend/eva2-server
backend/*.exe
*.test
*.out

# Frontend / Node
node_modules/
dist/
build/
coverage/

# Env files with real secrets — this also catches Vite's .env.local and
# .env.production variants — but keep the .env.example templates tracked
.env
.env.*
*.env
!.env.example

# Merge / patch leftovers
*.orig
*.rej

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.log
